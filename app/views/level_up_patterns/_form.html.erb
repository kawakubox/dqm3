<%= form_with(model: level_up_pattern) do |form| %>
  <% if level_up_pattern.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(level_up_pattern.errors.count, "error") %> prohibited this level_up_pattern from being saved:</h2>

      <ul>
        <% level_up_pattern.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :lineage, style: "display: block" %>
    <%= form.select :lineage, Lineage.select_pairs %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
